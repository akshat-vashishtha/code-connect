apiVersion: v1
kind: Secret
metadata:
  name: codeconnect-secrets
  namespace: codeconnect-dev
type: Opaque
stringData:
  # Secrets reference for database & session security
  MONGO_ROOT_PASSWORD: "dev_secure_password"
  REDIS_PASSWORD: "dev_redis_password"
