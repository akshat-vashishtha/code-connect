apiVersion: v1
kind: ConfigMap
metadata:
  name: gateway-config
  namespace: codeconnect-dev
  labels:
    app: gateway-service
data:
  SERVER_PORT: "8080"
  REDIS_HOST: "redis"
  REDIS_PORT: "6379"
  USER_SERVICE_URI: "http://user-service:8080"
