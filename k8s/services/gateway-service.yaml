apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: codeconnect-dev
  labels:
    app: gateway-service
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: gateway-service
