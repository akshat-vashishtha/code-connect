apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: codeconnect-dev
  labels:
    app: user-service
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: user-service
