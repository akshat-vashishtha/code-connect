apiVersion: v1
kind: ConfigMap
metadata:
  name: mongodb-config # ConfigMap name; injected via configMapRef in mongodb-statefulset.yaml
  namespace: codeconnect-dev
  labels:
    app: mongodb
data:
  MONGO_INITDB_DATABASE: "codeconnect_db" # Default database name created on startup
